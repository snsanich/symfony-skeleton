php:
    build: '.'
    links:
        - database
    env_file: support/env/dev.env
    volumes:
        - '.:/srv'

front:
    build: 'front'
    ports:
        - 0.0.0.0:8085:80
    links:
        - php

database:
    image: postgres:9.4
    env_file: support/env/dev.env
    volumes_from:
        - dbdata

dbdata:
    image: postgres:9.4
    volumes:
        - /var/lib/postgresql
    command: "true"
